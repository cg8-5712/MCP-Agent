# Web Frontend

This directory will contain the frontend as a git submodule.

To add the frontend submodule:
```bash
git submodule add <frontend-repo-url> web
git submodule update --init --recursive
```
